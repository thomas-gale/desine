import React, {  } from "react";

const Replicate = (): JSX.Element => {
  return (
    <div className="flex flex-col p-4 space-y-2">
      <div className="alert alert-warning">
        <p>
					TODO - Take a nft.storage/other pining service API key. Iterate through all the nfts in the contract and pin them to the service.
        </p>
      </div>
    </div>
  );
};

export default Replicate;
